// start slingin' some d3 here.

var svg = d3.select('div').append('svg'); var asteroid = function(n){
//asteroid.png   var asteroids = [];   for(var i=0;i<n;i++){
asteroids.push(i);   }

  var circle = svg.append('svg').attr('width', 50).attr('height', 50).append('circle').attr('cx', 25).attr('cx', 25).attr('r', 25).style('fill', 'green');
}

asteroid(5);
